class Voting {
    public static void main(String[] args) {
        int myAge = 30;
        int votingAge = 18;
        String res = (myAge > votingAge) ? "Eligible to vote" : "Too young to vote";
        System.out.println(res);
    }
}
